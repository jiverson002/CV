\documentclass[a4paper,12pt]{article}

\usepackage[margin=1.0in]{geometry}
\usepackage[english]{babel}

% change appearance of lists
\usepackage{enumitem}
\setlist{%
  nosep,
}
\setlist[itemize,1]{%
  label={},
  leftmargin=0em,
}
\setlist[itemize,2]{%
  label={},
  leftmargin=1em,
  before*=\footnotesize,
}
\setlist[description,1]{%
  font=\normalfont,
  labelwidth=3em,
  leftmargin=!,
  labelsep=0em,
  labelindent=0em,
  align=left,
}

% suppress page numbering
\pagestyle{empty}

% suppress section numbers
\setcounter{secnumdepth}{0}

% prevent latex from breaking items across pages
\interlinepenalty10000

% modify header spacing
\usepackage{titlesec}
\titleformat{\section}{\Large\bfseries\filcenter}{}{}{}{\filcenter}
\titlespacing{\subsection}{0pt}{2\baselineskip}{\dimexpr-\parskip+\baselineskip}
\titlespacing{\subsubsection}{0pt}{\baselineskip}{-\parskip}

% used by pandoc, but unnecessary
\providecommand{\tightlist}{}

% undefine a column type, courtesy of
%   https://tex.stackexchange.com/questions/144434/re-newing-a-column-type
\newcommand\undefcolumntype[1]{\expandafter\let\csname NC@find@#1\endcsname\relax}

% create a table environment which suppresses horizontal rules and fills column
% width
\usepackage{longtable}
\usepackage{tabularx}
\newcommand\forcenewcolumntype[1]{\undefcolumntype{#1}\newcolumntype{#1}}
\forcenewcolumntype{l}{>{\raggedright\arraybackslash}p{.5\textwidth}}
\forcenewcolumntype{r}{>{\raggedleft\arraybackslash}p{.5\textwidth}}
\providecommand{\toprule}{}
\providecommand{\midrule}{}
\providecommand{\bottomrule}{\\}
\providecommand{\addlinespace}{}

% this is a hack until i can find a way to use \tightlist to change the
% \leftmargin of the description environment so that two different styles of
% description lists can be provided -- it requires the use of \tabto{...} in the
% markdown file
\usepackage{tabto}

\begin{document}
$body$
\end{document}
